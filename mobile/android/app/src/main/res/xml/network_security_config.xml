<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is denied everywhere by default.

  debug-overrides applies only when the app is built debuggable, so the development
  exemptions below cannot reach a release build even by accident. 10.0.2.2 is the
  Android emulator's alias for the host machine; localhost covers a reverse-forwarded
  port via `adb reverse`.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <debug-overrides>
        <domain-config cleartextTrafficPermitted="true">
            <domain includeSubdomains="false">10.0.2.2</domain>
            <domain includeSubdomains="false">localhost</domain>
            <domain includeSubdomains="false">127.0.0.1</domain>
        </domain-config>
    </debug-overrides>
</network-security-config>
